import 'package:flutter/material.dart';

class DoctorHomPage extends StatelessWidget {
  const DoctorHomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Doctor Screen'),
    );
  }
}