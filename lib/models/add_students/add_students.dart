import 'package:flutter/material.dart';

class AddStudentsPage extends StatelessWidget {
  const AddStudentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Add Students');
  }
}