import 'package:flutter/material.dart';

class AddEdecatorPage extends StatelessWidget {
  const AddEdecatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Add Edecatour');
  }
}